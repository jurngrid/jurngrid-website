# JurnGrid placeholder site

Static GitHub Pages bundle.

## Upload these files to the repository root
- `index.html`
- `styles.css`
- `CNAME`

GitHub Pages should publish from `main` → `/(root)`.

The `CNAME` file keeps the custom domain set to `jurngrid.com`.
